# M3-021 — Report Deduplication

Status: ACCEPTED

Implemented semantic report deduplication keyed by property, variant, and report input hash. Snapshot identity and renderer identity are intentionally excluded, allowing equivalent governed semantics from a newer snapshot to reuse the existing report version.

Acceptance evidence: dedicated unit tests and full repository suite.
