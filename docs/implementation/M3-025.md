# M3-025 — Report Semantic Diff Engine

Status: ACCEPTED

Adds deterministic semantic comparison between report payloads. Snapshot IDs, verification timestamps, lineage manifest hashes, and finding source-snapshot IDs are excluded from the semantic projection. Governed content changes are returned as stable changed paths, categories, and a canonical diff hash, with persistence through reporting.report_diffs.
