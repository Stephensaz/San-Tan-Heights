# M3-022 — Version Allocation & Persistence

Status: ACCEPTED

Implemented transaction-scoped report target locking, semantic deduplication before version allocation, concurrency-safe counter allocation, immutable report/dependency persistence, exact payload hashing, and lineage consistency checks. Configuration dependencies may be non-snapshot-owned; finding, Passport, and wording dependencies retain source snapshot lineage.

Acceptance evidence: dedicated unit/migration tests and full repository suite. Live PostgreSQL concurrency execution remains required before production certification.
