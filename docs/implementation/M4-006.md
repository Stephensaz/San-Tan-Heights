# M4-006 — Diagram Slot Binding

Status: ACCEPTED

Added governed diagram-slot binding against a locked diagram policy. A diagram must match the report property, requested canonical diagram type, APPROVED status, PASS QA, and all finding IDs required by the slot. Optional slots degrade safely; required slots fail closed. Every bound diagram emits an exact DIAGRAM presentation dependency with version, semantic fingerprint, and slot lineage.

Acceptance evidence: property/finding lineage matching, QA/approval enforcement, required-slot blocking, exact dependency capture, and full repository tests passed.
