# M1-013 — State Adapter Framework

Status: ACCEPTED

Implemented a persistence-only state-adapter boundary keyed by `(entity_type, state_dimension)`, plus a kernel test adapter using `SELECT ... FOR UPDATE` and compare-and-swap state/version updates. Business rules remain outside adapters. Added `operations.kernel_test_entities` as a governed test entity for M1 transition-engine certification.
