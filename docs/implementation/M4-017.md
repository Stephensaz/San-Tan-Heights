# M4-017 — Publication Channel Pointer Model

Status: ACCEPTED

Current delivery artifact resolution now reads the explicit `publication.channel_pointers` record for property + variant + channel. WEB, PDF_DOWNLOAD, and PRINT can therefore advance independently while preserving one semantic-current report pointer.

Acceptance evidence: channel pointer tests plus full repository certification passed.
