# M4-004 — Presentation Input Hash Engine

Status: ACCEPTED

Added deterministic presentation-input hashing over the immutable report identity plus render-specific contract/template/renderer versions and exact presentation dependencies. Dependencies are canonically ordered; duplicates fail closed. Operational metadata such as timestamps, worker IDs, and render version numbers is excluded. The engine intentionally does not alter semantic report identity.

Acceptance evidence: deterministic ordering, change sensitivity, audience/render-type separation, duplicate rejection, and full repository tests passed.
