# M3-001 — Regeneration Job Persistence Completion

Status: ACCEPTED

Implements durable regeneration job records, active-target uniqueness, queue/lease indexes, and guarded job-state shape constraints. Active logical work is unique by property + report variant + job target key while completed historical jobs remain preserved.

PostgreSQL-specific execution remains an environment-level certification gate until a live PostgreSQL 16+ instance is provisioned.
