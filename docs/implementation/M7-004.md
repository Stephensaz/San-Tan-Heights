# M7-004 — Environment Parity Verification

Accepted a locked environment-parity policy and deterministic verifier for material runtime attributes including application version, Python major/minor, PostgreSQL major version, timezone, locale, canonical-serialization version, and production-certification contract version. Missing required attributes or exact-match drift fail closed. Expected/observed profiles and fingerprints are persisted immutably.
