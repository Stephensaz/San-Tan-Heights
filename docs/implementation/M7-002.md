# M7-002 — Certification Candidate Freeze

Accepted a deterministic, immutable candidate-freeze record bound to the exact production-certification run candidate version/fingerprint, deployable artifact SHA-256, source revision, artifact URI, and operator identity. Candidate identity drift from the parent production run fails closed.

Persistence is protected by immutable PostgreSQL evidence triggers.
