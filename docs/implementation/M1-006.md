# M1-006 — Reference Tables & Seed Registry

Status: ACCEPTED

Implemented registry-backed, idempotent reference seed data without modifying the already-accepted M1-005 migrations. Added additive reference tables for event types, guard IDs, and data classifications plus machine-readable reference values for report variants, QA statuses, and render types.

Live PostgreSQL execution remains an environment-level gate; structural and registry reconciliation tests pass in this repository runtime.
