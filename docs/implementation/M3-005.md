# M3-005 — Worker Claim / Lease / Heartbeat

Status: ACCEPTED

Implemented concurrency-safe regeneration worker claiming with priority ordering and FOR UPDATE SKIP LOCKED, worker-scoped leases, CLAIMED→RUNNING startup, heartbeat extension, retry-ready scheduling, and lease ownership enforcement.

Live PostgreSQL execution of locking/lease behavior remains a production-certification environment gate.
