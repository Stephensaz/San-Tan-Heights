# M7-003 — Contract / Build Manifest Pinning

Accepted immutable pins for the exact CONTRACT-MANIFEST.yaml and BUILD-MANIFEST.yaml used by a production-certification run. Contract-manifest pinning verifies every locked contract file against its declared SHA-256 before the pin is accepted. Pin fingerprints include the exact manifest payload and byte hash.
