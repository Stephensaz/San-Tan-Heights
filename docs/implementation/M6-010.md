# M6-010 Private Media Authorization

Private media access is authorized independently of render binding. Media visibility maps to the locked data classifications, requires `property.media.read`, enforces property scope, and additionally requires APPROVED/CLEARED media evidence. Unknown visibility levels fail closed.
