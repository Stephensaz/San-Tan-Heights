# M4-009 — PDF Render Adapter

Status: ACCEPTED

Added deterministic PDF rendering using ReportLab invariant mode over a shared governed-text projection. The adapter uses the canonical report as its sole semantic content source and emits `application/pdf`; it does not create new semantic content or publish.

Acceptance evidence: valid PDF signature, deterministic bytes/hash, render-type/property validation, and full repository tests passed.
