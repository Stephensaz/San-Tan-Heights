# M9-011 — Refresh / Regeneration / Rollback Replay

Status: ACCEPTED

## Purpose

Replay the accepted M9 production activation through governed refresh, dependency-driven regeneration, controlled publication update, failure isolation, and rollback paths to prove changed records update only from governed dependency changes while unchanged records remain fingerprint-stable.

## Frozen inputs

- M9-001 through M9-010 accepted outputs.
- accepted M2 dependency-impact and snapshot controls.
- accepted M3 regeneration controls.
- accepted M4 publication-pointer and rollback semantics.
- accepted M7 production rollback / progressive automation controls.
- locked M9 Production Population & Community Activation contract.

## Hard rules

- unchanged records remain fingerprint-stable;
- changed records regenerate only from governed dependency changes;
- refresh may not change identity/phase/spatial facts without an upstream governed change;
- stale/unavailable states remain explicit;
- regeneration must preserve exact tier eligibility and lineage;
- failed refresh/regeneration items are isolated and retryable;
- rollback restores only a previously governed safe pointer state;
- immutable accepted history is never overwritten;
- invalidated history is never automatic fallback;
- no uncontrolled fleet replay or publication.

## Acceptance boundary

M9-011 is accepted only when deterministic replay proves governed changes propagate through exactly the valid dependency chain, unchanged properties and variants remain fingerprint-stable, failures remain isolated, publication pointers update safely, rollback restores the exact governed baseline, and no unauthorized recomputation, tier leakage, or historical mutation occurs.


## Implementation

M9-011 binds accepted M9 activation state to the already-accepted dependency-impact, regeneration, publication-pointer, and rollback controls.

Implemented artifacts:

- `registries/activation/m9-011-refresh-replay-v1.0.yaml`
- `src/activation/refresh_replay.py`
- `tests/unit/activation/test_refresh_replay.py`

The replay runtime proves:

- unimpacted property/tier fingerprints remain exactly stable;
- changed fingerprints are permitted only inside the declared impacted dependency scope;
- semantic-equivalent regeneration may remain a no-op;
- failed impacted items are isolated rather than contaminating unrelated properties;
- replay results are deterministic independent of input ordering;
- M9-010 publication pointer updates can be rolled back to the exact captured baseline;
- M2-019, M3-026, and M7-023 remain accepted prerequisites;
- waivers remain prohibited.

M9-011 remains `IN_PROGRESS` until live repository CI and inherited presentation-certification gates pass.


## Acceptance evidence

- M9 Activation CI run 51: PASS.
- M9 Architecture Boundary Validation run 52: PASS.
- initial failed run 50 correctly exposed legacy accepted-document parsing mismatch; implementation repaired without weakening prerequisites.
- unimpacted fingerprint drift tests: PASS.
- impacted governed-change tests: PASS.
- failure isolation tests: PASS.
- deterministic replay ordering: PASS.
- publication pointer rollback to exact baseline: PASS.
- M2-019 dependency-impact prerequisite: PASS.
- M3-026 regeneration prerequisite: PASS.
- M7-023 rollback prerequisite: PASS.
- M9-010 controlled-publication prerequisite: PASS.
- waivers: 0.
- deterministic repository binding fingerprint: `b2014b6d3ff1f9294855d04f6a466073266a89c902ca2074898c60d0f1cd21fb`.

**M9-011 verdict: ACCEPTED.**
