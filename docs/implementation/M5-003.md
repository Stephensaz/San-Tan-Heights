# M5-003 — Deterministic Membership Resolver

Accepted deterministic membership resolution. Exact scope filtering is applied before canonical ordering by property ID, report variant, and channel. Conflicting duplicate targets fail closed. The membership fingerprint represents the release population only and intentionally excludes downstream target IDs/fingerprints.
