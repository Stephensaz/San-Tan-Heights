# M3-007 — Variant-Specific Job Freshness

Status: ACCEPTED

Implemented `VariantFreshnessChecker` to compare the target snapshot and current accepted snapshot using only the requested audience semantic fingerprint. Agent-only changes therefore do not stale Seller/Public jobs; a relevant variant fingerprint change returns STALE. Missing target/current snapshots fail closed as BLOCKED.

Acceptance evidence: `tests/unit/regeneration/test_variant_freshness.py` and full repository suite.
