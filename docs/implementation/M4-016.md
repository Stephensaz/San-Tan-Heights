# M4-016 — Current Semantic Report Model

Status: ACCEPTED

Current semantic report resolution now reads the explicit `publication.report_current` pointer for property + variant. It never infers currentness using MAX(version), timestamps, or latest-created behavior.

Acceptance evidence: pointer-model tests plus full repository certification passed.
