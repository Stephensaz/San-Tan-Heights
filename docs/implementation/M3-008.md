# M3-008 — Report Persistence Completion

Status: ACCEPTED

Implemented `reporting.report_versions`, `reporting.report_dependencies`, lookup/reverse-dependency indexes, report-integrity constraints, and reserved immutable semantic diff persistence. `ReportRepository` validates SHA-256 identities and excludes failed/stale/invalidated/archived reports from equivalent-report reuse.

Acceptance evidence: report persistence/migration tests and full repository suite.
