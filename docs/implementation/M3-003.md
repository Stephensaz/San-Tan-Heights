# M3-003 — Job Target Key Engine

Status: ACCEPTED

Implements deterministic SHA-256 job target keys from property, audience variant, target snapshot, report schema, content contract, and variant-policy versions. Runtime timestamps, worker identity, and other operational metadata are intentionally excluded.
