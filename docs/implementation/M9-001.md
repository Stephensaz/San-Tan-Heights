# M9-001 — Production Corpus Manifest & Admission Bootstrap

Status: BLOCKED

## Purpose

Freeze the exact governed production property corpus and the admission contract before any production population begins.

## Required outputs

- immutable corpus manifest;
- deterministic corpus-manifest fingerprint;
- exact source snapshot references;
- canonical property identifier for every corpus member;
- admission-state enum;
- quarantine-reason enum;
- exclusion-reason enum;
- full accounting invariant;
- schema validation;
- negative controls for missing/duplicate property identity;
- negative controls for unclassified records;
- lineage binding to approved source snapshots.

## Admission states

- ADMITTED
- QUARANTINED
- EXCLUDED_WITH_GOVERNED_REASON

## Hard rules

- no property may be silently dropped;
- no property may appear twice;
- no address text may substitute for canonical identity;
- no unresolved identity may be admitted;
- no missing lineage may be admitted;
- no publication occurs in M9-001;
- no report generation occurs in M9-001;
- no new intelligence is derived in M9-001.

## Acceptance boundary

M9-001 is accepted only when the corpus manifest is deterministic, fully accounted, schema-valid, lineage-bound, and fail-closed under intentionally corrupted membership and admission fixtures.


## Implementation state

The admission/bootstrap machinery is implemented:

- production corpus manifest JSON Schema;
- typed admission/quarantine/exclusion states;
- exact source-snapshot lineage references;
- duplicate identity rejection;
- exact declared-total accounting;
- unknown-lineage rejection;
- state-specific governed reason enforcement;
- deterministic manifest fingerprint;
- dedicated M9 activation CI;
- source-candidate audit registry.

## Provenance audit

The currently available project-library artifacts were reviewed for use as the production membership source.

Rejected candidates:

1. `San_Tan_Heights_Canonical_Property_Master_v0_1.xlsx`
   - Explicitly contains phase shells/placeholders.
   - States that exact residential APN/address enumeration was incomplete.
   - Not eligible to freeze as the production corpus.

2. `report_regeneration_registry_v1.csv`
   - 742 audience-tier rows were observed.
   - Rows are organized by AGENT / SELLER / PUBLIC output tier.
   - Represents a partial report-regeneration working set, not a one-row-per-property complete corpus.

3. `san_tan_heights_lot_location_verification_evidence_ledger_v1.csv`
   - Selective evidence ledger for researched fields/properties.
   - Not a complete membership roster.

## Blocking dependency

M9-001 cannot be accepted until the authoritative full canonical San Tan Heights production property roster is available as a frozen source snapshot with:

- one canonical property identifier per corpus member;
- exact source snapshot identity;
- cryptographic source hash;
- complete membership;
- enough provenance to classify every member as ADMITTED, QUARANTINED, or EXCLUDED_WITH_GOVERNED_REASON.

No corpus count will be hard-coded or reconstructed from incomplete historical artifacts.

## Current decision

**BLOCKED — authoritative full production roster snapshot not yet located in the accessible project/repository sources.**

The correct repair is to provide or locate that upstream canonical roster, then load it through the already-implemented M9-001 manifest gate. M9-002 must not begin before M9-001 is accepted.
