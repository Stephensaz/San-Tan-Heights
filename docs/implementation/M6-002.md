# M6-002 — Service Authentication Boundary

Accepted a fail-closed service authentication boundary based on short-lived HMAC-SHA256 signed service assertions with externally supplied key material. The boundary validates registry status, allowed method, audience, bounded lifetime, nonce, credential resolution, constant-time signature comparison, and post-authentication capability checks. A claimed service ID alone never establishes identity.
