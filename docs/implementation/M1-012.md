# M1-012 — Guard Runtime

Status: ACCEPTED

Implemented a registry-backed, read-only, fail-closed guard runtime. The initial governed guards are `ENTITY_EXISTS`, `EXPECTED_STATE_MATCHES`, and `CALLER_AUTHORIZED`. A guard exception becomes `ERROR`; remaining guards are recorded as `NOT_EVALUATED`. Guard execution never mutates domain state.
