# M3-024 — READY Immutability

Status: ACCEPTED

READY report semantic evidence is database-protected against mutation. Operational lifecycle fields may still change, but canonical payload, semantic hashes, source snapshot, schema/policy versions, report identity/version, and generation semantics cannot be rewritten after READY. Dependency rows of READY reports cannot be updated or deleted.
