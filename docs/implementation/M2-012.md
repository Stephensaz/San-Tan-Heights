# M2-012 — Snapshot Diff Engine

Status: ACCEPTED

Deterministic diffing of immutable snapshot findings and dependencies. Unchanged findings are omitted; semantic vs non-semantic dependency changes are distinguished and the diff hash is canonical.
