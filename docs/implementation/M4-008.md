# M4-008 — WEB Render Adapter

Status: ACCEPTED

Added deterministic WEB rendering from the canonical report and governed presentation request. The adapter escapes consumer-facing text, preserves canonical section/card/finding structure, and emits UTF-8 HTML with report/property/presentation identity metadata. It does not derive facts, rewrite approved wording, or publish.

Acceptance evidence: deterministic output/hash, HTML escaping, property/request boundary validation, and full repository tests passed.
