# M6-009 Negative-Field Protection

Audience-specific recursive deny lists provide defense in depth after whitelist DTO construction. Public and Seller responses reject snapshot lineage, semantic fingerprints, internal evidence detail, Passport/internal status fields, and schema-contract internals as applicable. Agent responses still reject restricted/system-secret fields. Unknown audiences fail closed.
