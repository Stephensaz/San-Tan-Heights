# M4-020 — Pre-Publication Variant Freshness

Status: ACCEPTED

Added a final audience-specific semantic freshness check immediately before publication pointer mutation. The check reuses the accepted variant fingerprint contract so Agent-only semantic change does not stale Seller/Public publication, while relevant semantic drift blocks or stales publication.

Contract references: STH-SERVICE-CONTRACT-v1.0, STH-PERSISTENCE-v1.0.

Acceptance evidence: freshness tests plus full repository suite passed.
