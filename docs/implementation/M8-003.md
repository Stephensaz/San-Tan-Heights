# M8-003 — Shared Component Primitives

Status: ACCEPTED

## Acceptance

Immutable, deterministic shared primitives are implemented for text, stacks, cards, dividers, and disclosures. Semantic override properties are rejected so the component layer cannot create rankings, valuation, desirability, or audience/publication overrides.
