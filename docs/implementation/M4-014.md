# M4-014 — Storage Integrity

Status: ACCEPTED

Added independent verification of bytes retrieved from artifact storage against the immutable database artifact hash and byte length. Missing storage evidence, size drift, or hash drift fails closed.

Acceptance evidence: storage integrity unit tests plus full repository certification passed.
