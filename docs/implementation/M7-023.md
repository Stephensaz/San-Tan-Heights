# M7-023 — Cohort Rollback / Containment

Accepted in 0.1.47.

Rollback is driven only from the immutable M7-022 baseline. Entries execute in reverse deterministic order. A restore failure does not continue as if successful: the affected property is automatically contained and the run is recorded as CONTAINED with immutable per-target evidence.
