# M5-002 — Release Policy Registry

Accepted implementation of `STH-RELEASE-POLICY-v1.0`. Release policy is machine-readable and fail-closed, defining allowed variants/channels, canonical membership ordering, duplicate handling, and the requirement that fleet membership be frozen before rollout.
