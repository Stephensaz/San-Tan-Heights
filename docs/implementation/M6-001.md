# M6-001 — Service Identity Registry

Accepted a locked machine-readable service identity registry for the modular-monolith runtime. Each service has one stable service ID, trust tier, dedicated database role, allowed authentication method, and explicit capability set. Human roles remain separate and are not represented as services.
