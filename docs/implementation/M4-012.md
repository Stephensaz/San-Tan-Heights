# M4-012 — Render Validation

Status: ACCEPTED

Added a fail-closed render validation gate covering locked render-contract version, allowed MIME type, artifact hash/size integrity, storage locator presence, READY/CLEAN/PASS state, explicit publication eligibility, and continued eligibility of the immutable source report. Validation has no publication authority.

Acceptance evidence: focused validation tests plus full repository certification passed.
