# M3-006 — Retry Classification & Recovery

Status: ACCEPTED

Implemented a fail-closed retry policy registry and recovery service. Transient database/storage/worker/timeout/deadlock failures retry with bounded exponential delay; deterministic governance/schema/classification failures BLOCK; exhausted transient retries terminate as FAILED.

Live PostgreSQL execution of locking/lease behavior remains a production-certification environment gate.
