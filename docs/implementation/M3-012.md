# M3-012 — Report Section Registry

Status: ACCEPTED

Implemented the canonical ordered report section registry with allowed/required variants, finding-type mappings, and governed fallback behavior. The runtime cross-validates section requirements against all variant policies and fails closed on unknown or inconsistent sections.

Acceptance evidence: dedicated unit tests and full repository suite.
