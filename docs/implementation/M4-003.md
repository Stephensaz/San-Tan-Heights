# M4-003 — Render Contract Registry

Status: ACCEPTED

Added locked `STH-RENDER-CONTRACTS-v1.0` coverage for WEB, PDF, PRINT, and MOBILE_PREVIEW. Each contract defines its artifact kind, allowed MIME types, publication channels, exact required presentation inputs, and forbidden semantic behaviors. Unknown render types and malformed contract definitions fail closed.

Acceptance evidence: registry coverage and fail-closed tests passed; full repository suite passed.
