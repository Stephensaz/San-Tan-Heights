# M3-020 — Canonical Payload Hashing

Status: ACCEPTED

Implemented deterministic SHA-256 hashing over the exact canonical JSON report payload after canonical report schema and cross-reference validation. Equivalent object-key order hashes identically, while lineage/content changes alter the payload integrity hash.

Acceptance evidence: dedicated unit tests and full repository suite.
