# M6-003 — Database Least-Privilege Completion

Accepted dedicated NOLOGIN PostgreSQL roles and service-specific grants across snapshot, report, render, publication, regeneration, release, operations, audit, and public-delivery responsibilities. PUBLIC schema/object access is revoked, future migration-owned objects default private, release orchestration cannot directly mutate publication pointers, and public delivery can read only current PUBLIC projections rather than historical reporting base tables. Live PostgreSQL permission execution remains required in production certification.
