# M2-001 Governed-State Read Contract

Status: ACCEPTED

Implements the validated anti-corruption boundary `GetGovernedPropertyState`. Snapshot Service receives governed production intelligence only, with source-read token, immutable Passport references, semantic fingerprints, audience wording, and dependency scopes. Unknown statuses, malformed fingerprints, and cross-property responses fail closed.
