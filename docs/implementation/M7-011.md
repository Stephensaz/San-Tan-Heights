# M7-011 — Shadow Acceptance Engine

Status: ACCEPTED

Implemented a fail-closed shadow acceptance gate over immutable shadow-cycle and manual-audit evidence.

Key controls:
- requires three consecutive latest PASS cycles under the locked policy;
- requires shadow membership to match the frozen pilot population;
- requires a manual-audit sample for the latest cycle;
- requires complete audit coverage and PASS-only audit outcomes;
- emits deterministic acceptance evidence with exact cycle/sample lineage.
