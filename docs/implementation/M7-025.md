# M7-025 — Certification Revocation

Accepted in 0.1.47.

Certification revocation is append-only, candidate-bound evidence. It never rewrites a prior approval or evidence bundle. Downstream approval logic treats any matching active revocation as a hard blocker.
