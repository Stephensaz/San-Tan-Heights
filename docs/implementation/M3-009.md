# M3-009 — Report Version Counter

Status: ACCEPTED

Implemented `reporting.report_version_counters` keyed by property and audience variant. `ReportVersionAllocator` creates/locks the counter row with `FOR UPDATE`, returns the current version, and atomically advances it. `MAX(version)+1` is explicitly not used.

Acceptance evidence: `tests/unit/report_builder/test_version_allocator.py` and full repository suite.
