# M4-018 — Publication Staging

Status: ACCEPTED

Added durable, idempotent publication staging keyed to the exact property + variant + channel + report + render target. Staging uses explicit compare-and-set lifecycle transitions and remains separate from current-publication pointers.

Contract references: STH-PERSISTENCE-v1.0, STH-SERVICE-CONTRACT-v1.0.

Acceptance evidence: staging unit/persistence tests plus full repository suite passed.
