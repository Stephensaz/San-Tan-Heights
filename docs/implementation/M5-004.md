# M5-004 — Release Manifest Freezer

Accepted immutable release-manifest freezing. The manifest captures policy/version, exact release scope, membership fingerprint, deterministic membership ordinals, target snapshot/report/render IDs, and semantic/presentation fingerprints. PostgreSQL triggers prevent population/target-evidence mutation after freeze while preserving later item lifecycle fields.
